# roc_desk-sql standalone build

Run from the repository root:

`powershell
.\build-standalone.ps1
`

The release executable is written to in\roc_desk-sql.exe. Use
-Configuration debug for a fast development build.
